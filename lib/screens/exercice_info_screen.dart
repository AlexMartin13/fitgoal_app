import 'package:flutter/material.dart';

class ExerciceInfo extends StatelessWidget {
  const ExerciceInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}